{
  "scripts": {
    "format": "yarn format-prettier && yarn format-html",
    "format-prettier": "prettier --write **/*.{md,json}",
    "format-html": "html-beautify -r -f *.html"
  },
  "devDependencies": {
    "js-beautify": "^1.7.5",
    "prettier": "^1.11.1"
  }
}
